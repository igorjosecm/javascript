/*
function somar(x,y) {
    return x + y;
}
*/

/*
let somar = function(x, y) {
    return x + y;
}
*/

/*
let somar = (x, y) => {
    return x + y
};
*/

let somar = (x, y) => x + y;

//letrasNoNome = nome => nome.length;

let letrasNoNome = nome => {
    let numero = nome.length;
    return numero;
}

console.log(letrasNoNome("igor"));